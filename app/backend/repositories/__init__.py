"""Persistence layer — the only modules that write `sdr_devices` (architecture §4.4).

Empty placeholder as of Phase 0 — owned by Phase 1B (the database-engineer
track), which also builds `models.py`, `db.py` and `alembic/`. Its concrete
`DeviceRepository` should satisfy the Protocol frozen at
`app.backend.interfaces.repository.DeviceRepository`.
"""
