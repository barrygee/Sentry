"""Concrete implementations of `interfaces/` Protocols (architecture §4.2).

Empty placeholder as of Phase 0 — owned entirely by Phase 1A (the
hardware-edge track): `sysfs_usb`, `fixture_usb`, `udev_netlink`,
`reconcile_hotplug`, `composite_hotplug`, `asyncio_process`, `fake_process`,
`ctypes_rtlsdr`, `fake_rtlsdr`, `net`, `system_clock`.
"""
